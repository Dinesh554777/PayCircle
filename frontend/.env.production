# Production API base URL — points the deployed Vercel frontend at the Render backend.
# Vite inlines this at build time (vite build => production mode), so it MUST be set
# here or as a Vercel build env var for the deployed app to reach the backend.
VITE_API_URL=https://paycircle-5l6n.onrender.com/api
